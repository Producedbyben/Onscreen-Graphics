{
  "name": "onscreen-graphics-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "dev:web": "npm --workspace @onscreen/web run dev",
    "typecheck": "npm run --workspaces typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
